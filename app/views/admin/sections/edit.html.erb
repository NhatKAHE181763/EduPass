<%= turbo_frame_tag dom_id(@section), class: "list-group-item p-3 bg-light border-start border-4 border-primary" do %>
  <h5 class="fw-bold mb-3">Chỉnh sửa Section</h5>
  <%= render "form", section: @section %>
<% end %>
